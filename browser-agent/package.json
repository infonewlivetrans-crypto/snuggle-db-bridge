{
  "name": "radius-track-browser-agent",
  "version": "0.2.7",
  "private": true,
  "description": "Radius Track Browser Agent — MV3 extension. Does NOT use ATI API.",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "build:stable": "RT_CHANNEL=stable node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit",
    "package": "node scripts/package-extension.mjs",
    "package:stable": "RT_CHANNEL=stable node esbuild.config.mjs && RT_CHANNEL=stable node scripts/package-extension.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
